# Authentication Flow Diagram

## Request Flow - Protected vs Unprotected Routes

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          CLIENT REQUEST                                      │
└────────────────────────────┬────────────────────────────────────────────────┘
                             │
                    ┌────────▼────────┐
                    │  Is request to  │
                    │  /api/* route?  │
                    └────────┬────────┘
                             │
        ┌────────────────────┴────────────────────┐
        │                                         │
        ▼                                         ▼
    ┌─────────────────────┐         ┌──────────────────────┐
    │ PROTECTED ROUTES    │         │ UNPROTECTED ROUTES   │
    │ (/api/user/*)       │         │ (/api/auth/*)        │
    │ (/api/chat/*)       │         │ (/api/webhooks/*)    │
    │ (/api/pusher/*)     │         │ (/api/cron/*)        │
    └────────┬────────────┘         └──────────┬───────────┘
             │                                 │
             ▼                                 ▼
    ┌──────────────────────────┐   ┌─────────────────────────┐
    │ MIDDLEWARE AUTH CHECK    │   │ SPECIALIZED AUTH        │
    │ (Auth0)                  │   │ (Signature/Token)       │
    │                          │   │                         │
    │ Has Auth0 session?       │   │ Verify webhook sig?     │
    │ ├─ YES → Continue        │   │ Verify cron secret?     │
    │ └─ NO → Redirect to      │   │ ├─ VALID → Continue     │
    │         login (302)      │   │ └─ INVALID → 401        │
    └────────┬─────────────────┘   └──────────┬──────────────┘
             │                                │
             ▼                                │
    ┌──────────────────────────┐             │
    │ ROUTE HANDLER            │             │
    │ (app/api/*/route.ts)     │             │
    │                          │             │
    │ Call requireUser()?       │             │
    │ ├─ YES → Verify again     │             │
    │ └─ NO → Proceed           │             │
    └────────┬─────────────────┘             │
             │                               │
             └───────────────┬───────────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │ PROCESS REQUEST │
                    │ & RESPOND       │
                    └─────────────────┘
```

---

## Authentication Matrix

```
┌──────────────────┬─────────────────────────────────────────────────────────┐
│ Route Category   │ Middleware Check │ Handler Check │ Signature Check      │
├──────────────────┼──────────────────┼───────────────┼──────────────────────┤
│ UI Routes        │ ✅ Auth0 Session │ N/A           │ N/A                  │
│ (/learn/*)       │                  │               │                      │
├──────────────────┼──────────────────┼───────────────┼──────────────────────┤
│ User APIs        │ ✅ Auth0 Session │ ✅ requireUser│ N/A                  │
│ (/api/user/*)    │                  │               │                      │
├──────────────────┼──────────────────┼───────────────┼──────────────────────┤
│ Chat API         │ ✅ Auth0 Session │ ✅ requireUser│ N/A                  │
│ (/api/chat/*)    │                  │               │                      │
├──────────────────┼──────────────────┼───────────────┼──────────────────────┤
│ Pusher API       │ ✅ Auth0 Session │ ✅ requireUser│ ✅ Channel validation│
│ (/api/pusher/*)  │                  │ + validation  │                      │
├──────────────────┼──────────────────┼───────────────┼──────────────────────┤
│ Webhooks         │ ❌ Excluded      │ N/A           │ ✅ Signature         │
│ (/api/webhooks)  │                  │               │    (Stripe)          │
├──────────────────┼──────────────────┼───────────────┼──────────────────────┤
│ Cron Jobs        │ ❌ Excluded      │ N/A           │ ✅ Bearer Token      │
│ (/api/cron/*)    │                  │               │    (CRON_SECRET)     │
├──────────────────┼──────────────────┼───────────────┼──────────────────────┤
│ Auth0 Callback   │ ❌ Excluded      │ N/A           │ ✅ Auth0 Callback    │
│ (/api/auth/*)    │                  │               │    Verification      │
└──────────────────┴──────────────────┴───────────────┴──────────────────────┘
```

---

## Middleware Configuration Visualization

```
middleware.ts Configuration:

┌────────────────────────────────────────────────────────────┐
│ config.matcher = [                                         │
│   '/learn/:path*',          ← UI Routes                   │
│   '/leaderboard/:path*',    ← UI Routes                   │
│   '/quests/:path*',         ← UI Routes                   │
│   '/shop/:path*',           ← UI Routes                   │
│   '/lesson/:path*',         ← UI Routes                   │
│   '/api/user/:path*',       ← API Routes (NEW)            │
│   '/api/chat/:path*',       ← API Routes (NEW)            │
│   '/api/pusher/:path*',     ← API Routes (NEW)            │
│ ]                                                         │
│                                                           │
│ NOT INCLUDED (uses other auth):                          │
│ • /api/auth/* → Auth0 handles                           │
│ • /api/webhooks/* → Signature verification              │
│ • /api/cron/* → Bearer token                            │
└────────────────────────────────────────────────────────────┘
```

---

## Security Improvement Timeline

```
BEFORE (Vulnerable):
┌──────────────────────┐
│ /api/user/profile    │──────────────────────▶ NO AUTH CHECK ❌
└──────────────────────┘

┌──────────────────────┐
│ /api/chat            │──────────────────────▶ NO AUTH CHECK ❌
└──────────────────────┘

┌──────────────────────┐
│ /api/pusher/auth     │──────────────────────▶ NO AUTH CHECK ❌
└──────────────────────┘


AFTER (Fixed):
┌──────────────────────┐         ┌──────────────────┐
│ /api/user/profile    │────────▶│ Middleware Auth  │
└──────────────────────┘         └────────┬─────────┘
                                          │
                                          ▼
                                 ┌──────────────────┐
                                 │ Handler Auth     │
                                 │ requireUser()    │
                                 └────────┬─────────┘
                                          │
                                          ▼
                                 ✅ REQUEST ALLOWED

┌──────────────────────┐         ┌──────────────────┐
│ /api/chat            │────────▶│ Middleware Auth  │
└──────────────────────┘         └────────┬─────────┘
                                          │
                                          ▼
                                 ┌──────────────────┐
                                 │ Handler Auth     │
                                 │ requireUser()    │
                                 └────────┬─────────┘
                                          │
                                          ▼
                                 ✅ REQUEST ALLOWED

┌──────────────────────┐         ┌──────────────────┐
│ /api/pusher/auth     │────────▶│ Middleware Auth  │
└──────────────────────┘         └────────┬─────────┘
                                          │
                                          ▼
                                 ┌──────────────────┐
                                 │ Handler Auth     │
                                 │ requireUser()    │
                                 │ + validation     │
                                 └────────┬─────────┘
                                          │
                                          ▼
                                 ✅ REQUEST ALLOWED
```

---

## Defense in Depth Strategy

```
Layer 1: MIDDLEWARE LEVEL
┌──────────────────────────────────────────────────────┐
│ Next.js Middleware (runs on edge)                     │
│ Protects: UI routes + specific API routes            │
│ Benefit: Rejects unauthenticated requests before     │
│          reaching route handlers                     │
└────────────────────┬─────────────────────────────────┘
                     │
                     ▼
Layer 2: ROUTE HANDLER LEVEL
┌──────────────────────────────────────────────────────┐
│ API Route Handler (server-side)                       │
│ Protects: Sensitive endpoints with requireUser()     │
│ Benefit: Catches any bypass attempts, provides       │
│          user context for authorization              │
└────────────────────┬─────────────────────────────────┘
                     │
                     ▼
Layer 3: BUSINESS LOGIC LEVEL
┌──────────────────────────────────────────────────────┐
│ Database Queries & Validations                        │
│ Protects: Ensures user can only access own data      │
│ Benefit: Prevents horizontal privilege escalation    │
└──────────────────────────────────────────────────────┘

Example: Pusher Channel Authorization
Layer 1: Middleware ensures user is authenticated
         │
Layer 2: Handler checks user exists via requireUser()
         │
Layer 3: Business logic validates user is in the match
         ├─ SELECT * FROM challengeMatches
         │  WHERE id = ? AND (playerOneId = ? OR playerTwoId = ?)
         └─ Returns 403 Forbidden if user not in match
```

---

## API Cost Protection (Chat Example)

```
BEFORE (No User Tracking):
User A: $$$$$$$$ (High costs, unmetered)
User B: (No tracking)
User C: (No tracking)
Total: Unpredictable costs

AFTER (With User Authentication):
User A: $50 (can set rate limit)
User B: $25 (can monitor)
User C: $35 (can alert on spikes)
Total: Controlled & predictable

Benefits:
├─ Per-user rate limiting
├─ Quota enforcement  
├─ Usage monitoring & alerts
├─ Prevent abuse/bot attacks
└─ Cost prediction
```

---

## Testing Strategy

```
Test Case Matrix:

┌─────────────────────────────────────────────────────────┐
│ Test: Unauthenticated Request to /api/user/profile      │
├─────────────────────────────────────────────────────────┤
│ Expected: 302 Redirect or 401 Unauthorized              │
│ Status: ✅ PASS (middleware rejects)                    │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ Test: Authenticated Request to /api/user/profile        │
├─────────────────────────────────────────────────────────┤
│ Expected: 200 OK with user data                         │
│ Status: ✅ PASS (both layers allow)                     │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ Test: Webhook to /api/webhooks/stripe (no user session) │
├─────────────────────────────────────────────────────────┤
│ Expected: 200 OK (with valid Stripe signature)          │
│ Status: ✅ PASS (excluded from middleware, sig verified)│
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ Test: Cron to /api/cron (with valid CRON_SECRET)        │
├─────────────────────────────────────────────────────────┤
│ Expected: 200 OK                                        │
│ Status: ✅ PASS (excluded from middleware, token valid) │
└─────────────────────────────────────────────────────────┘
```

---

## Summary

**Key Improvements:**
1. ✅ Middleware now protects 3 additional sensitive API routes
2. ✅ Chat API now requires user authentication
3. ✅ Defense-in-depth with dual authentication layers
4. ✅ All design decisions documented with rationale
5. ✅ Security checklist created for ongoing verification

**Status: COMPLETE** 🔒
